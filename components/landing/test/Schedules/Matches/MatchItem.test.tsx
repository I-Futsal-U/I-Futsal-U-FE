import { render } from "@testing-library/react";

import MatchItem from "@/components/landing/Schedules/Matches/MatchItem";
// 기능 완성 후, 작성 예정

describe("MatchItem test", () => {
  test("MatchItem 렌더링 테스트", () => {
    render(<MatchItem />);
  });
});
