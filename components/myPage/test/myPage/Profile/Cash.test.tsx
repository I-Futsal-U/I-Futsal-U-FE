import Cash from "@/components/myPage/Profile/Cash";

describe("Cash Component Test", () => {
  test("보유 캐시가 렌더링 된다", () => {
    // 추후 작성
  });

  test("캐시 충전 버튼을 누르면 캐시 충전 페이지로 넘어간다.", () => {
    // 추후 작성
  });
});
